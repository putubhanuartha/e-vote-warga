export enum StatusFormFilling {
    ready = "ready",
    active = "active",
    done = "done"
}
export enum InputFormType {
    text = "text",
    textarea = "textarea",
    checkbox = "checkbox",
    radio = "radio"
}
export enum JenisPilihan {
    rt = "rt",
    rw = "rw"
}